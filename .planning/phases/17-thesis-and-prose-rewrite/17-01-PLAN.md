---
phase: 17-thesis-and-prose-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/preview/decades-to-days.mdx
autonomous: true
requirements:
  - THES-01
  - COMP-01
  - COMP-03

must_haves:
  truths:
    - "The core thesis (agents cannot sustain execution without externalized state) is stated or strongly implied within the first two paragraphs of the article"
    - "The hackathon section is noticeably shorter than before, with fewer domain lists and tighter prior-attempts summary"
    - "Domain enumeration appears at most once (not twice across Hackathon paras 1 and 4)"
  artifacts:
    - path: "src/content/preview/decades-to-days.mdx"
      provides: "Rewritten opening and compressed hackathon"
      contains: "externalize"
  key_links:
    - from: "Opening paragraph"
      to: "The Session Problem section"
      via: "Thesis thread: sustainability/externalized state mentioned in opening, developed in Session Problem"
      pattern: "sustain|externalize|persist"
---

<objective>
Rewrite the opening paragraph to land the thesis before 25% of the article, and compress the Hackathon section from four verbose paragraphs to two or three tight ones.

Purpose: The thesis currently appears at 41% of the article. THES-01 requires it before 25%. The Hackathon section has redundant domain lists and verbose prior-attempts enumeration that dilute the spine.
Output: Modified src/content/preview/decades-to-days.mdx with thesis-forward opening and compressed hackathon.
</objective>

<execution_context>
@/Users/wawen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wawen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-outline-and-cut-map/CUT-MAP.md
@.planning/phases/16-outline-and-cut-map/OUTLINE.md
@src/content/preview/decades-to-days.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite opening paragraph with thesis pivot</name>
  <files>src/content/preview/decades-to-days.mdx</files>
  <action>
Rewrite the opening paragraph of the article following CUT-MAP.md's REWRITE directive for the opening.

Current opening (line 10):
"Over three days, four of us, working half-time between other commitments, shipped ten production-ready agent skills..."

The rewrite must accomplish:
1. KEEP the hook: the scale (three days, ten skills) is compelling. Preserve the first one or two sentences or compress them into one.
2. ADD a thesis pivot within this paragraph or as a brief second paragraph. The reader must understand by the end of the opening that the real discovery was not speed but sustainability: agents could build, but sustaining that ability across sessions required externalizing state.
3. REFRAME the question: change from "how did we go fast?" to "what made it possible to sustain this beyond a weekend?" The current "The question worth asking is: what changed?" is too vague and echoes a "The question was..." framing that appears again in The Session Problem para 3 (see CUT-MAP cross-cutting note on repeated ideas).
4. The thesis must be stated or strongly foreshadowed BEFORE 25% of the article (~415 words). Since the opening is the first paragraph, this is achievable.
5. Do NOT use em dashes in any form (project CLAUDE.md rule). Use commas, colons, semicolons, parentheses, or separate sentences instead.

The center of gravity (from OUTLINE.md): "agents can execute, but they cannot sustain execution without externalized state."

Target: the opening should be roughly the same length (~100-120 words) but with the thesis present.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/content/preview/decades-to-days.mdx','utf8'); const lines=c.split('\n'); const bodyStart=lines.findIndex(l=>l==='---',1)+1; const body=lines.slice(bodyStart).join(' '); const words=body.trim().split(/\s+/); const first100=words.slice(0,100).join(' ').toLowerCase(); const has=first100.includes('sustain')||first100.includes('externalize')||first100.includes('persist')||first100.includes('session'); console.log('Thesis signal in first 100 words:',has); if(!has) process.exit(1); const emDash=c.includes('—')||c.includes(' -- '); console.log('Em dash free:',!emDash); if(emDash) process.exit(1); console.log('PASS');"</automated>
  </verify>
  <done>Opening paragraph contains a thesis signal (sustainability/externalized state/session continuity) within the first 100 words. No em dashes anywhere in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Compress Hackathon section (4 paragraphs to 2-3)</name>
  <files>src/content/preview/decades-to-days.mdx</files>
  <action>
Compress the four Hackathon paragraphs following CUT-MAP.md's COMPRESS directives. The section currently runs ~480 words; target ~280 words.

Paragraph 1 ("If you run a platform serving thousands of engineers..."):
- COMPRESS (COMP-01, COMP-03). Reduce domain enumeration from five specific scenarios to two or three, or replace with a single characterizing sentence.
- Keep: opening setup about operational toil and closing insight about toil being inherent to scale.
- Target: ~120 words to ~80 words.

Paragraph 2 ("This is the problem that motivated the hackathon..."):
- COMPRESS (COMP-01, COMP-03). Compress three prior attempts into a single sentence listing them, then state the shared flaw once.
- Replace per-attempt detail ("A Slackbot that queried platform APIs. A chatbot wrapper with function calling over the same APIs. An MCP server exposing tool endpoints.") with something like: "A Slackbot, a chatbot wrapper, an MCP server. Different interfaces, same flaw."
- Keep the diagnosis: "Without curated base context... answers were generic."
- Target: ~130 words to ~70 words.

Paragraph 3 ("The fourth attempt changed the approach entirely..."):
- COMPRESS (COMP-01). Keep "The fourth attempt changed the approach entirely" and the core idea about a complete protocol.
- Compress the middle: reader does not need full enumeration of protocol steps.
- The sentence about manual context management as the bottleneck can be shortened or dropped (the rewritten opening now establishes this theme).
- Target: ~120 words to ~70 words.

Paragraph 4 ("Three days, four engineers, ten skills..."):
- COMPRESS (COMP-01, COMP-03). Cut or heavily compress the domain list (already enumerated in paragraph 1; second enumeration is redundant per CUT-MAP cross-cutting note).
- Keep: summary line ("Three days, four engineers, ten skills") and the inversion insight.
- KEEP the bridge sentence: "But it also created a new problem: managing context across sessions..." (critical transition to The Session Problem).
- Target: ~110 words to ~60 words.

Cross-cutting notes to honor:
- Domain enumeration should appear at most once across all four paragraphs, not twice.
- Tone should shift toward more personal/reflective, less formal/expository (CUT-MAP tone note).
- Do NOT use em dashes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/content/preview/decades-to-days.mdx','utf8'); const hackStart=c.indexOf('## The Hackathon'); const sessStart=c.indexOf('## The Session Problem'); const hack=c.substring(hackStart,sessStart); const words=hack.trim().split(/\s+/).length; console.log('Hackathon section words:',words); const ok=words<=320; console.log('Under 320 words:',ok); if(!ok) process.exit(1); const emDash=c.includes('—')||c.includes(' -- '); console.log('Em dash free:',!emDash); if(emDash) process.exit(1); console.log('PASS');"</automated>
  </verify>
  <done>Hackathon section is 320 words or fewer (down from ~480). Domain lists appear at most once. Bridge sentence to The Session Problem is preserved. No em dashes.</done>
</task>

</tasks>

<verification>
1. Thesis (externalized state / session continuity) is stated or strongly implied within the first two paragraphs
2. Hackathon section is measurably shorter (~280-320 words vs original ~480)
3. Domain enumeration does not appear twice
4. The bridge to The Session Problem ("But it also created a new problem...") survives
5. No em dashes anywhere in the file
6. File remains at src/content/preview/decades-to-days.mdx
7. `npm run build` passes (MDX renders without errors)
</verification>

<success_criteria>
- The core thesis appears before 25% of the article (within first ~415 words)
- Hackathon section is noticeably shorter with tighter enumeration
- The article still reads as a coherent narrative from hook to Session Problem
</success_criteria>

<output>
After completion, create `.planning/phases/17-thesis-and-prose-rewrite/17-01-SUMMARY.md`
</output>
