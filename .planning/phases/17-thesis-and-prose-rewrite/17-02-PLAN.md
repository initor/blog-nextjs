---
phase: 17-thesis-and-prose-rewrite
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/content/preview/decades-to-days.mdx
autonomous: true
requirements:
  - THES-02
  - THES-03
  - THES-04
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "'An agent with no memory is not a collaborator' has structural emphasis: standalone paragraph or section-opening sentence"
    - "'The context boundary was not the conversation window. It was the working directory.' has structural emphasis: standalone paragraph or section-closing standalone sentence"
    - "The Session Problem reads as the spine: every section before it sets it up, every section after resolves or extends it"
    - "Planning-file enumeration (ROADMAP, STATE, CONTEXT, PLAN, SUMMARY) is rendered as a compact scannable artifact, not a run-on sentence"
    - "Session Problem paragraphs 8-9 (manual workflow, GSD) are noticeably shorter than before"
  artifacts:
    - path: "src/content/preview/decades-to-days.mdx"
      provides: "Rewritten Session Problem with structural emphasis and compressed prose"
      contains: "not a collaborator"
  key_links:
    - from: "Session Problem para 3 (THES-02 line)"
      to: "Opening thesis signal"
      via: "The 'not a collaborator' line deepens what the opening established"
      pattern: "not a collaborator"
    - from: "Session Problem para 6 (THES-03 line)"
      to: "Conclusion"
      via: "Context boundary insight resolves the spine and flows into the closing three-era arc"
      pattern: "working directory"
---

<objective>
Rewrite The Session Problem section to give THES-02 and THES-03 structural emphasis, convert the planning-file enumeration to a compact scannable artifact, and compress the manual-workflow/GSD paragraphs. Strengthen the spine (THES-04) throughout.

Purpose: The Session Problem is the essay's spine. Two key quotable lines are currently buried in longer paragraphs. The planning-file enumeration reads as a run-on sentence. The manual/GSD paragraphs are the weakest spine section and need compression.
Output: Modified src/content/preview/decades-to-days.mdx with structurally emphasized thesis lines, compact planning-file artifact, and tighter prose.
</objective>

<execution_context>
@/Users/wawen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wawen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-outline-and-cut-map/CUT-MAP.md
@.planning/phases/16-outline-and-cut-map/OUTLINE.md
@.planning/phases/17-thesis-and-prose-rewrite/17-01-SUMMARY.md
@src/content/preview/decades-to-days.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural emphasis for THES-02 and THES-03, spine strengthening, and planning-file artifact</name>
  <files>src/content/preview/decades-to-days.mdx</files>
  <action>
Rewrite The Session Problem section following CUT-MAP.md directives. This task handles paragraphs 1-6 (transition through "context boundary" insight) plus the planning-file enumeration.

**Paragraph 1** ("The hackathon worked. But it also surfaced a problem..."):
- KEEP. Clean two-sentence transition. No changes.

**Paragraph 2** ("When you close an agent session and open a new one..."):
- REWRITE (THES-04). Keep core mechanism (ephemeral context, everything lost). Sharpen language. Compress the hackathon qualification to half a sentence. Make the closing hit harder: first session makes progress, second starts from scratch. This should feel like a punch, not a summary.

**Paragraph 3** ("This is not a minor inconvenience..."):
- REWRITE (THES-02, THES-04). Extract "An agent with no memory is not a collaborator" into a STANDALONE PARAGRAPH. It must not be buried mid-paragraph. Options:
  a) Make it a standalone single-sentence paragraph preceding the rest of the argument, OR
  b) Make it the opening sentence of a new paragraph that then develops the point.
- Compress "the shared understanding of what has been tried, what has been decided, what comes next" enumeration.
- Keep closing pivot: "The question now was whether they could sustain execution across time."

**Paragraph 4** ("The solution, when it emerged, was almost embarrassingly simple..."):
- REWRITE (COMP-02, COMP-03). Convert the five-file enumeration into a COMPACT SCANNABLE ARTIFACT.
- Keep framing: "If the context window is ephemeral, externalize the state into files that persist."
- Convert the five files (ROADMAP.md, STATE.md, CONTEXT.md, PLAN.md, SUMMARY.md) from inline prose to one of:
  a) A tight bulleted list with one-line descriptions each (preferred for MDX), OR
  b) An inline protocol notation like: "ROADMAP (milestones) > STATE (position) > CONTEXT (decisions) > PLAN (tasks) > SUMMARY (handoff)"
- Keep: "Not documentation written for humans, but memory written for agents."
- Keep: "These files are not bureaucratic overhead. They are the mechanism by which one session hands off to the next."

**Paragraph 5** ("What surprised me was how quickly this compounded..."):
- KEEP. One of the strongest paragraphs. No changes.

**Paragraph 6** ("This pattern of distilling what matters..."):
- REWRITE (THES-03). Give "The context boundary was not the conversation window. It was the working directory." STRUCTURAL EMPHASIS.
- Make it a standalone paragraph or a section-closing standalone sentence.
- Keep "starting ahead, not starting over" formulation.
- Compress the rest to serve as setup for the standalone THES-03 line.

Cross-cutting:
- Do NOT use em dashes in any form.
- Maintain first-person, reflective voice (avoid product-description tone).
- The spine (THES-04) should feel like it intensifies through this section, not merely explains.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/content/preview/decades-to-days.mdx','utf8'); const lines=c.split('\n'); const checks=[]; const t2=lines.some((l,i)=>{const t=l.trim(); return (t.startsWith('An agent with no memory') && t.length<120 && (i===0||lines[i-1].trim()==='')); }); checks.push(['THES-02 standalone',t2]); const t3=lines.some((l,i)=>{const t=l.trim(); return t.includes('context boundary was not the conversation window') && (i===lines.length-1||lines[i+1].trim()===''); }); checks.push(['THES-03 emphasis',t3]); const hasArtifact=c.includes('ROADMAP')&&c.includes('STATE')&&c.includes('SUMMARY')&&(c.includes('- **ROADMAP')||c.includes('- ROADMAP')||c.includes('**ROADMAP')||c.includes('ROADMAP (')); checks.push(['Planning-file artifact',hasArtifact]); const emDash=c.includes('\u2014')||c.includes(' -- '); checks.push(['Em dash free',!emDash]); let pass=true; checks.forEach(([n,v])=>{console.log(n+':',v?'PASS':'FAIL'); if(!v) pass=false;}); if(!pass) process.exit(1); console.log('ALL PASS');"</automated>
  </verify>
  <done>THES-02 line is a standalone paragraph (preceded and followed by blank lines). THES-03 line has structural emphasis (standalone or section-closing). Planning files rendered as a compact artifact (not run-on prose). No em dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Compress paragraphs 7-9 (generalization, manual workflow, GSD)</name>
  <files>src/content/preview/decades-to-days.mdx</files>
  <action>
Compress the final three paragraphs of The Session Problem following CUT-MAP.md directives. These are the weakest spine section per the spine-strength analysis.

**Paragraph 7** ("That insight unlocked something I had not planned for..."):
- COMPRESS (COMP-03). Reduce the three non-coding examples to two, or fold into a single sentence.
- Keep: insight that the pattern generalizes beyond coding.
- Keep: "The working directory became the universal context boundary."
- Target: ~100 words to ~60 words.

**Paragraph 8** ("The manual version of this workflow was effective but tedious..."):
- COMPRESS (COMP-03). Cut the repeated detail about ChatGPT drafting protocols (already covered in Hackathon para 3 per cross-cutting note).
- Keep: the key progression point that the workflow was manual and tedious, and the pattern had not yet become infrastructure.
- This paragraph should serve primarily as a bridge to the GSD paragraph.
- Target: ~100 words to ~40 words.

**Paragraph 9** ("Then I found GSD..."):
- COMPRESS (COMP-03). Tighten the evolution arc.
- Compress the "What I had been doing manually" list.
- Keep: GSD introduction and closing insight ("had become infrastructure that any project could use from the first minute").
- Avoid product-description tone (CUT-MAP tone note: "GSD handled automatically" reads like feature marketing).
- Target: ~100 words to ~60 words.

Overall target for paragraphs 7-9 combined: ~300 words down to ~160 words.

Cross-cutting:
- Do NOT use em dashes.
- Maintain reflective, first-person voice. Avoid marketing/product-description tone.
- These paragraphs should feel like the spine extending and resolving, not going slack.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/content/preview/decades-to-days.mdx','utf8'); const sessStart=c.indexOf('## The Session Problem'); const concStart=c.indexOf('## Conclusion'); const sess=c.substring(sessStart,concStart); const words=sess.trim().split(/\s+/).length; console.log('Session Problem section words:',words); const ok=words<=620; console.log('Under 620 words:',ok); if(!ok) process.exit(1); const emDash=c.includes('\u2014')||c.includes(' -- '); console.log('Em dash free:',!emDash); if(emDash) process.exit(1); const total=c.trim().split(/\s+/).length; console.log('Total article words:',total); console.log('PASS');"</automated>
  </verify>
  <done>Session Problem section is 620 words or fewer (down from ~760+). Paragraphs 7-9 combined are ~160 words. No product-description tone. No em dashes. The article's total word count is measurably lower than the original ~1661.</done>
</task>

</tasks>

<verification>
1. "An agent with no memory is not a collaborator" is a standalone paragraph or section-opening sentence (not buried mid-paragraph)
2. "The context boundary was not the conversation window. It was the working directory." has structural emphasis
3. Planning-file enumeration is a compact scannable artifact (bulleted list, table, or protocol notation), not a run-on sentence
4. Session Problem paragraphs 7-9 are measurably shorter (~160 words combined vs ~300 original)
5. The Session Problem reads as the spine: it intensifies through the section rather than going slack
6. No em dashes anywhere in the file
7. File remains at src/content/preview/decades-to-days.mdx
8. `npm run build` passes
</verification>

<success_criteria>
- THES-02 and THES-03 lines both have structural emphasis (visually distinct from surrounding prose)
- Planning-file enumeration is scannable, not prose-embedded
- Session Problem section is tighter overall, especially the final three paragraphs
- The spine (THES-04) is felt throughout: each paragraph either sets up, develops, or resolves the thesis
</success_criteria>

<output>
After completion, create `.planning/phases/17-thesis-and-prose-rewrite/17-02-SUMMARY.md`
</output>
